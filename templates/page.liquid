<div class="page-width">
  <div class="grid">
    <div class="grid__item container m-auto border-none">
      <div class="section-header text-center">
        <h1>{{ page.title }}</h1>
      </div>

      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
</div>